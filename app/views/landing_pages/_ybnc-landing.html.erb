<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>YBNC — Young Black Nameless Coalition</title>
<meta name="description" content="The Young Black Nameless Coalition — fostering unity, dismantling systemic barriers, and amplifying Black voices. Led by Ashley Dorelus. Vallejo, CA.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=Source+Serif+4:ital,wght@0,300;0,400;0,600;1,300;1,400&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">

<style>
/* ═══════════════════════════════════════════════════════════════
   YBNC — Design System
   Aesthetic: Movement Editorial · Black Arts · Liberation Press
   "Named by the unnamed. Built for the unseen."
   ═══════════════════════════════════════════════════════════════ */

:root {
  /* ── Color System ── */
  --black:       #0A0A0A;
  --charcoal:    #141414;
  --dark:        #1C1C1C;
  --dark-mid:    #262626;
  --border:      #2E2E2E;
  --border-lit:  #3D3D3D;

  --red:         #B91C1C;   /* liberation red — urgent, alive */
  --red-deep:    #991818;
  --red-glow:    rgba(185,28,28,0.12);

  --gold:        #C9A84C;   /* Black excellence, Afrocentric royalty */
  --gold-lite:   #E0C278;
  --gold-mist:   rgba(201,168,76,0.1);

  --cream:       #F5F0E8;   /* newspaper, warm, human */
  --cream-dark:  #DDD5C4;
  --white:       #FAFAF8;

  --text-primary:   #F5F0E8;
  --text-secondary: #9A9488;
  --text-muted:     #5C5650;
  --text-on-red:    #FFFFFF;
  --text-on-gold:   #0A0A0A;
  --text-on-cream:  #0A0A0A;

  /* ── Typography ── */
  --font-display: 'Syne', sans-serif;
  --font-body:    'Source Serif 4', Georgia, serif;
  --font-mono:    'DM Mono', monospace;

  /* ── Spacing ── */
  --sp-1:  0.25rem;
  --sp-2:  0.5rem;
  --sp-3:  0.75rem;
  --sp-4:  1rem;
  --sp-6:  1.5rem;
  --sp-8:  2rem;
  --sp-12: 3rem;
  --sp-16: 4rem;
  --sp-20: 5rem;
  --sp-24: 6rem;
  --sp-32: 8rem;

  /* ── Sharp geometry ── */
  --r: 0px;
  --r-sm: 2px;

  /* ── Motion ── */
  --t-fast: 0.15s ease;
  --t-base: 0.3s ease;
  --t-slow: 0.55s cubic-bezier(0.16, 1, 0.3, 1);
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  background: var(--black);
  color: var(--text-primary);
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
  line-height: 1.6;
}

img   { display: block; max-width: 100%; }
a     { color: inherit; text-decoration: none; }
ul    { list-style: none; }
button { cursor: pointer; border: none; background: none; font: inherit; }

/* ── Grain texture ── */
body::before {
  content: '';
  position: fixed; inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='g'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23g)' opacity='0.035'/%3E%3C/svg%3E");
  pointer-events: none; z-index: 9999; opacity: 0.5;
}

.container { max-width: 1280px; margin: 0 auto; padding: 0 var(--sp-8); }
.section   { padding: var(--sp-24) 0; }

/* ── Typography ── */
.headline {
  font-family: var(--font-display);
  font-weight: 800; line-height: 0.92;
  letter-spacing: -0.02em; text-transform: uppercase;
}
.mono-label {
  font-family: var(--font-mono);
  font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase;
  color: var(--gold); display: flex; align-items: center; gap: 0.6rem;
}
.mono-label::before { content: '▪'; font-size: 0.5rem; }

/* ── Reveal animation ── */
.reveal {
  opacity: 0; transform: translateY(30px);
  transition: opacity 0.9s ease, transform 0.9s var(--t-slow);
}
.reveal.vis { opacity: 1; transform: none; }
.d1 { transition-delay: 0.1s; } .d2 { transition-delay: 0.22s; }
.d3 { transition-delay: 0.34s; } .d4 { transition-delay: 0.46s; }


/* ═══════════════════════════════════════════════════════════════
   NAVIGATION
   ═══════════════════════════════════════════════════════════════ */

.nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 200;
  padding: 1.25rem var(--sp-8);
  display: flex; align-items: center; justify-content: space-between;
  transition: all 0.4s ease;
}
.nav.scrolled {
  background: rgba(10,10,10,0.96);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
  padding: 0.9rem var(--sp-8);
}

.nav__brand {
  display: flex; flex-direction: column; line-height: 1;
}
.nav__brand-abbr {
  font-family: var(--font-display);
  font-size: 1.3rem; font-weight: 800;
  letter-spacing: 0.06em; color: var(--cream);
}
.nav__brand-full {
  font-family: var(--font-mono);
  font-size: 0.55rem; letter-spacing: 0.15em;
  text-transform: uppercase; color: var(--text-muted);
  margin-top: 1px;
}

.nav__links {
  display: flex; align-items: center; gap: 2.5rem;
}
.nav__link {
  font-family: var(--font-display);
  font-size: 0.82rem; font-weight: 600;
  letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--text-secondary);
  position: relative; transition: color var(--t-fast);
}
.nav__link::after {
  content: ''; position: absolute; bottom: -3px; left: 0; right: 0;
  height: 1px; background: var(--gold);
  transform: scaleX(0); transform-origin: left;
  transition: transform var(--t-base);
}
.nav__link:hover { color: var(--cream); }
.nav__link:hover::after { transform: scaleX(1); }

.nav__cta {
  background: var(--red); color: var(--text-on-red);
  font-family: var(--font-display); font-size: 0.8rem;
  font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase;
  padding: 0.6rem 1.4rem;
  transition: background var(--t-fast), transform var(--t-fast);
}
.nav__cta:hover { background: var(--red-deep); transform: translateY(-1px); }

.nav__ham {
  display: none; flex-direction: column; gap: 5px;
}
.nav__ham span {
  display: block; width: 22px; height: 1.5px;
  background: var(--cream); border-radius: 0;
  transition: all 0.3s;
}


/* ═══════════════════════════════════════════════════════════════
   HERO
   ═══════════════════════════════════════════════════════════════ */

.hero {
  min-height: 100svh;
  position: relative; overflow: hidden;
  display: flex; flex-direction: column;
  background: var(--black);
}

/* Vertical red stripe — liberation flag energy */
.hero__stripe {
  position: absolute; top: 0; left: 7rem; bottom: 0;
  width: 3px; background: var(--red); opacity: 0.4;
}
.hero__stripe-gold {
  position: absolute; top: 0; left: calc(7rem + 8px); bottom: 0;
  width: 3px; background: var(--gold); opacity: 0.2;
}

/* Background glow */
.hero__glow {
  position: absolute; top: 0; right: -20%; width: 60vw; height: 100%;
  background: radial-gradient(ellipse at top right,
    rgba(185,28,28,0.06) 0%, transparent 55%);
  pointer-events: none;
}

.hero__content {
  position: relative; z-index: 2;
  padding: 9rem var(--sp-8) var(--sp-16);
  max-width: 1280px; margin: 0 auto; width: 100%;
  display: flex; flex-direction: column; justify-content: flex-end;
  flex: 1;
}

.hero__mono-label {
  opacity: 0; animation: fadeUp 0.8s 0.2s forwards ease;
  margin-bottom: var(--sp-6);
}

/* The big statement */
.hero__title {
  font-family: var(--font-display);
  font-size: clamp(4.5rem, 11vw, 11rem);
  font-weight: 800; line-height: 0.88;
  letter-spacing: -0.025em; text-transform: uppercase;
  color: var(--cream); margin-bottom: var(--sp-8);
}

.hero__title-line {
  display: block; overflow: hidden;
}
.hero__title-word {
  display: inline-block;
  opacity: 0; transform: translateY(110%);
  animation: wordReveal 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}
.hero__title-word:nth-child(1) { animation-delay: 0.3s; }
.hero__title-word:nth-child(2) { animation-delay: 0.5s; }
.hero__title-word:nth-child(3) { animation-delay: 0.7s; }
.hero__title-word:nth-child(4) { animation-delay: 0.9s; }

.hero__title em {
  font-style: normal;
  color: var(--red);
  -webkit-text-stroke: 0px;
}
.hero__title .outline {
  -webkit-text-stroke: 2px var(--cream);
  color: transparent;
}

@keyframes wordReveal {
  to { opacity: 1; transform: translateY(0); }
}
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: none; }
}

.hero__bottom {
  display: flex; align-items: flex-end; justify-content: space-between;
  gap: 3rem; padding-top: var(--sp-8);
  border-top: 1px solid var(--border);
  opacity: 0; animation: fadeUp 0.8s 1.1s forwards ease;
}

.hero__desc {
  font-family: var(--font-body);
  font-size: 1.1rem; font-weight: 300; line-height: 1.75;
  color: var(--text-secondary); max-width: 400px;
  font-style: italic;
}
.hero__desc strong { font-weight: 600; color: var(--cream); font-style: normal; }

.hero__actions { display: flex; gap: 1rem; align-items: center; flex-shrink: 0; }

/* Established line — bottom */
.hero__footer-bar {
  background: var(--red);
  padding: 0.75rem var(--sp-8);
  display: flex; align-items: center; justify-content: space-between;
  position: relative; z-index: 2;
  opacity: 0; animation: fadeUp 0.6s 1.3s forwards ease;
}
.hero__footer-bar-text {
  font-family: var(--font-mono);
  font-size: 0.68rem; letter-spacing: 0.18em;
  text-transform: uppercase; color: rgba(255,255,255,0.8);
}
.hero__footer-bar-text strong { color: white; }


/* ═══════════════════════════════════════════════════════════════
   MARQUEE
   ═══════════════════════════════════════════════════════════════ */

.marquee {
  background: var(--charcoal); border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  padding: 0.9rem 0; overflow: hidden; white-space: nowrap;
}
.marquee__track {
  display: inline-flex;
  animation: marquee 30s linear infinite;
}
@keyframes marquee {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}
.marquee__item {
  display: inline-flex; align-items: center; gap: 1.5rem;
  padding: 0 2.5rem;
  font-family: var(--font-display);
  font-size: 0.8rem; font-weight: 700;
  letter-spacing: 0.16em; text-transform: uppercase;
  color: var(--text-secondary);
}
.marquee__item--gold { color: var(--gold); }
.marquee__sep { color: var(--red); font-size: 0.5rem; }


/* ═══════════════════════════════════════════════════════════════
   MISSION STATEMENT
   ═══════════════════════════════════════════════════════════════ */

.mission { background: var(--black); border-bottom: 1px solid var(--border); }

.mission__grid {
  display: grid; grid-template-columns: 1fr 2fr;
  gap: 6rem; align-items: start;
}

.mission__label-col {
  padding-top: 0.4rem;
  display: flex; flex-direction: column; gap: var(--sp-8);
}

.mission__counter {
  font-family: var(--font-display);
  font-size: 6rem; font-weight: 800;
  color: var(--red); line-height: 1;
  opacity: 0.15;
}

.mission__side-text {
  font-family: var(--font-mono);
  font-size: 0.65rem; letter-spacing: 0.15em; text-transform: uppercase;
  color: var(--text-muted); line-height: 2;
  writing-mode: vertical-rl;
  transform: rotate(180deg);
}

.mission__statement {
  font-family: var(--font-display);
  font-size: clamp(2rem, 4.5vw, 4.2rem);
  font-weight: 700; line-height: 1.08;
  letter-spacing: -0.01em; color: var(--cream);
}

.mission__statement em {
  font-style: italic; font-weight: 400;
  color: var(--gold);
  font-family: var(--font-body);
  font-size: 0.88em;
}

.mission__body {
  font-size: 1.05rem; font-weight: 300; line-height: 1.9;
  color: var(--text-secondary); margin-top: var(--sp-8);
  max-width: 680px;
}

.mission__pull-quote {
  margin-top: var(--sp-8);
  padding: var(--sp-6) var(--sp-8);
  border-left: 3px solid var(--red);
  background: var(--charcoal);
}
.mission__pull-quote-text {
  font-family: var(--font-body);
  font-size: 1.15rem; font-style: italic; font-weight: 300;
  color: var(--cream); line-height: 1.65;
}
.mission__pull-quote-attr {
  font-family: var(--font-mono);
  font-size: 0.65rem; letter-spacing: 0.14em; text-transform: uppercase;
  color: var(--gold); margin-top: var(--sp-3);
}


/* ═══════════════════════════════════════════════════════════════
   FOCUS AREAS
   ═══════════════════════════════════════════════════════════════ */

.focus { background: var(--charcoal); border-top: 1px solid var(--border); }

.focus__header {
  margin-bottom: var(--sp-12);
}

/* Asymmetric grid — editorial layout */
.focus__grid {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  grid-template-rows: auto auto;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
}

.focus-card {
  padding: 2.5rem;
  background: var(--charcoal);
  position: relative; overflow: hidden;
  transition: background var(--t-base);
  cursor: default;
}
.focus-card:hover { background: var(--dark); }

.focus-card--1 { grid-column: 1 / 6; }  /* political advocacy */
.focus-card--2 { grid-column: 6 / 10; } /* community action */
.focus-card--3 { grid-column: 10 / 13; }/* Black liberation */
.focus-card--4 { grid-column: 1 / 5; }  /* empowerment */
.focus-card--5 { grid-column: 5 / 9; }  /* documentary */
.focus-card--6 { grid-column: 9 / 13; } /* systemic change */

.focus-card--accent {
  background: var(--red) !important;
}
.focus-card--accent:hover { background: var(--red-deep) !important; }
.focus-card--accent .focus-card__title { color: white; }
.focus-card--accent .focus-card__desc  { color: rgba(255,255,255,0.75); }
.focus-card--accent .focus-card__num   { color: rgba(255,255,255,0.4); }

.focus-card--gold-accent {
  background: var(--gold) !important;
}
.focus-card--gold-accent .focus-card__title { color: var(--black); }
.focus-card--gold-accent .focus-card__desc  { color: rgba(0,0,0,0.65); }
.focus-card--gold-accent .focus-card__num   { color: rgba(0,0,0,0.3); }

.focus-card__num {
  font-family: var(--font-mono);
  font-size: 0.62rem; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--text-muted); margin-bottom: var(--sp-6);
}

.focus-card__icon {
  font-size: 2rem; margin-bottom: var(--sp-4); line-height: 1;
}

.focus-card__title {
  font-family: var(--font-display);
  font-size: 1.4rem; font-weight: 700;
  text-transform: uppercase; letter-spacing: 0.02em;
  color: var(--cream); line-height: 1.1;
  margin-bottom: var(--sp-3);
}

.focus-card__desc {
  font-size: 0.9rem; font-weight: 300; line-height: 1.7;
  color: var(--text-secondary);
}

/* Decorative large background text */
.focus-card::after {
  content: attr(data-bg);
  position: absolute; bottom: -1rem; right: -0.5rem;
  font-family: var(--font-display);
  font-size: 5rem; font-weight: 900;
  color: rgba(255,255,255,0.02); line-height: 1;
  pointer-events: none; user-select: none;
  text-transform: uppercase;
}


/* ═══════════════════════════════════════════════════════════════
   ABOUT ASHLEY
   ═══════════════════════════════════════════════════════════════ */

.about { background: var(--black); }

.about__grid {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 0; align-items: stretch;
}

.about__image-col {
  position: relative; min-height: 600px;
  background: var(--dark);
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}

/* Photographic placeholder with editorial framing */
.about__photo-bg {
  position: absolute; inset: 0;
  background:
    radial-gradient(ellipse at 40% 35%, rgba(185,28,28,0.25) 0%, transparent 55%),
    radial-gradient(ellipse at 70% 70%, rgba(201,168,76,0.08) 0%, transparent 50%),
    linear-gradient(160deg, #1C1C1C 0%, #0A0A0A 100%);
}
.about__photo-frame {
  position: absolute;
  inset: 1.5rem;
  border: 1px solid rgba(201,168,76,0.2);
}
.about__photo-corner {
  position: absolute; width: 20px; height: 20px;
  border-color: var(--gold); border-style: solid;
}
.about__photo-corner--tl { top: -1px; left: -1px; border-width: 2px 0 0 2px; }
.about__photo-corner--tr { top: -1px; right: -1px; border-width: 2px 2px 0 0; }
.about__photo-corner--bl { bottom: -1px; left: -1px; border-width: 0 0 2px 2px; }
.about__photo-corner--br { bottom: -1px; right: -1px; border-width: 0 2px 2px 0; }

.about__photo-placeholder {
  position: relative; z-index: 2;
  font-size: 5rem; opacity: 0.15;
}

/* Award badge */
.about__award {
  position: absolute; bottom: 2rem; left: 2rem; right: 2rem;
  z-index: 3;
  background: var(--gold); color: var(--text-on-gold);
  padding: 1rem 1.25rem;
  display: flex; align-items: center; gap: 0.75rem;
}
.about__award-icon { font-size: 1.4rem; flex-shrink: 0; }
.about__award-title {
  font-family: var(--font-mono);
  font-size: 0.6rem; letter-spacing: 0.18em; text-transform: uppercase;
  color: rgba(0,0,0,0.6); margin-bottom: 0.2rem;
}
.about__award-name {
  font-family: var(--font-display);
  font-size: 0.88rem; font-weight: 700;
  text-transform: uppercase; color: var(--black);
}

.about__content {
  padding: 4rem 4rem 4rem 5rem;
  background: var(--charcoal);
  border-left: 1px solid var(--border);
  display: flex; flex-direction: column; justify-content: center;
}

.about__title {
  font-family: var(--font-display);
  font-size: clamp(2rem, 4vw, 3.5rem);
  font-weight: 800; text-transform: uppercase;
  letter-spacing: -0.01em; color: var(--cream);
  line-height: 0.95; margin: var(--sp-4) 0 var(--sp-6);
}
.about__title em { font-style: italic; color: var(--red); }

.about__body {
  font-size: 1rem; font-weight: 300; line-height: 1.9;
  color: var(--text-secondary); margin-bottom: var(--sp-4);
}

.about__tags {
  display: flex; flex-wrap: wrap; gap: 0.4rem; margin: var(--sp-6) 0;
}
.about__tag {
  padding: 0.3rem 0.8rem;
  border: 1px solid var(--border-lit);
  font-family: var(--font-mono);
  font-size: 0.65rem; letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--text-muted);
  transition: all var(--t-fast);
}
.about__tag:hover { border-color: var(--gold); color: var(--gold); }

.about__links {
  display: flex; gap: var(--sp-4); margin-top: var(--sp-6);
}


/* ═══════════════════════════════════════════════════════════════
   STATS / IMPACT
   ═══════════════════════════════════════════════════════════════ */

.impact {
  background: var(--red);
  padding: var(--sp-12) 0;
  position: relative; overflow: hidden;
}
.impact::before {
  content: 'YBNC';
  position: absolute; right: -1rem; top: 50%;
  transform: translateY(-50%);
  font-family: var(--font-display);
  font-size: 20vw; font-weight: 900;
  color: rgba(255,255,255,0.04); line-height: 1;
  pointer-events: none; user-select: none;
}

.impact__grid {
  display: grid; grid-template-columns: repeat(4,1fr);
  gap: 1px; background: rgba(255,255,255,0.1);
  position: relative; z-index: 1;
}

.impact-stat {
  background: var(--red);
  padding: 2.5rem 2rem;
  text-align: center;
  transition: background var(--t-fast);
}
.impact-stat:hover { background: var(--red-deep); }

.impact-stat__num {
  font-family: var(--font-display);
  font-size: 3.5rem; font-weight: 900;
  color: white; line-height: 1; margin-bottom: 0.4rem;
}
.impact-stat__label {
  font-family: var(--font-mono);
  font-size: 0.65rem; letter-spacing: 0.18em;
  text-transform: uppercase; color: rgba(255,255,255,0.65);
}
.impact-stat__desc {
  font-size: 0.82rem; font-weight: 300;
  color: rgba(255,255,255,0.5); margin-top: 0.4rem;
  line-height: 1.4;
}


/* ═══════════════════════════════════════════════════════════════
   GET INVOLVED
   ═══════════════════════════════════════════════════════════════ */

.involved { background: var(--charcoal); border-top: 1px solid var(--border); }

.involved__header {
  text-align: center; max-width: 640px;
  margin: 0 auto var(--sp-12);
}

.involved__grid {
  display: grid; grid-template-columns: repeat(3,1fr);
  gap: 1px; background: var(--border);
  border: 1px solid var(--border);
}

.involve-card {
  background: var(--charcoal);
  padding: 3rem 2.5rem;
  position: relative; overflow: hidden;
  transition: background var(--t-base);
}
.involve-card:hover { background: var(--dark); }
.involve-card::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 3px;
  background: var(--gold); transform: scaleX(0); transform-origin: left;
  transition: transform var(--t-slow);
}
.involve-card:hover::before { transform: scaleX(1); }

.involve-card__num {
  font-family: var(--font-mono); font-size: 0.62rem;
  letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--text-muted); margin-bottom: var(--sp-6);
}

.involve-card__icon {
  font-size: 2.5rem; margin-bottom: var(--sp-5); line-height: 1;
}

.involve-card__title {
  font-family: var(--font-display);
  font-size: 1.7rem; font-weight: 800;
  text-transform: uppercase; color: var(--cream);
  line-height: 1; margin-bottom: var(--sp-3);
  letter-spacing: 0.02em;
}

.involve-card__desc {
  font-size: 0.9rem; font-weight: 300; line-height: 1.75;
  color: var(--text-secondary); margin-bottom: var(--sp-6);
}

.involve-card--donate { background: var(--black); }
.involve-card--donate:hover { background: var(--charcoal); }
.involve-card--donate .involve-card__title { color: var(--gold); }


/* ═══════════════════════════════════════════════════════════════
   EVENTS
   ═══════════════════════════════════════════════════════════════ */

.events { background: var(--black); border-top: 1px solid var(--border); }

.events__header {
  display: flex; align-items: flex-end; justify-content: space-between;
  margin-bottom: var(--sp-8); gap: 2rem;
}

.events__list { display: flex; flex-direction: column; }

.event-row {
  display: flex; align-items: center; gap: 2rem;
  padding: 1.75rem 0;
  border-bottom: 1px solid var(--border);
  transition: all var(--t-base); cursor: pointer;
  position: relative;
}
.event-row:first-child { border-top: 1px solid var(--border); }
.event-row:hover { padding-left: 1rem; }
.event-row::before {
  content: '';
  position: absolute; left: 0; top: 0; bottom: 0;
  width: 0; background: var(--red);
  transition: width var(--t-base);
}
.event-row:hover::before { width: 3px; }

.event-row__date {
  width: 56px; height: 56px;
  background: var(--dark);
  border: 1px solid var(--border);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.event-row__date-day {
  font-family: var(--font-display); font-size: 1.4rem; font-weight: 900;
  color: var(--red); line-height: 1;
}
.event-row__date-month {
  font-family: var(--font-mono); font-size: 0.55rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--text-muted);
}

.event-row__info { flex: 1; min-width: 0; }
.event-row__title {
  font-family: var(--font-display);
  font-size: 1.1rem; font-weight: 700;
  text-transform: uppercase; letter-spacing: 0.04em;
  color: var(--cream); margin-bottom: 0.3rem;
}
.event-row__meta {
  font-family: var(--font-mono); font-size: 0.65rem;
  letter-spacing: 0.08em; color: var(--text-muted);
  display: flex; gap: 1.5rem; flex-wrap: wrap;
}

.event-row__type {
  padding: 0.2rem 0.7rem;
  font-family: var(--font-mono); font-size: 0.6rem;
  letter-spacing: 0.1em; text-transform: uppercase;
  border: 1px solid;
}
.event-type--advocacy { color: var(--red); border-color: var(--red); background: var(--red-glow); }
.event-type--community{ color: var(--gold); border-color: var(--gold); background: var(--gold-mist); }
.event-type--education{ color: var(--text-secondary); border-color: var(--border); }


/* ═══════════════════════════════════════════════════════════════
   NEWSLETTER / JOIN
   ═══════════════════════════════════════════════════════════════ */

.newsletter {
  background: var(--cream); color: var(--text-on-cream);
  padding: var(--sp-24) 0;
  position: relative; overflow: hidden;
}
.newsletter::before {
  content: 'JOIN';
  position: absolute; right: -2rem; top: 50%;
  transform: translateY(-50%);
  font-family: var(--font-display);
  font-size: 24vw; font-weight: 900;
  color: rgba(0,0,0,0.04); line-height: 1;
  pointer-events: none; user-select: none;
}

.newsletter__inner { position: relative; z-index: 1; }

.newsletter__label {
  font-family: var(--font-mono); font-size: 0.68rem;
  letter-spacing: 0.2em; text-transform: uppercase;
  color: var(--red); margin-bottom: var(--sp-4);
  display: flex; align-items: center; gap: 0.5rem;
}
.newsletter__label::before { content: '▪'; }

.newsletter__title {
  font-family: var(--font-display);
  font-size: clamp(2.5rem, 7vw, 6rem);
  font-weight: 900; line-height: 0.88;
  text-transform: uppercase; letter-spacing: -0.01em;
  color: var(--black); margin-bottom: var(--sp-6);
}
.newsletter__title em { color: var(--red); font-style: normal; }

.newsletter__sub {
  font-size: 1rem; font-weight: 300; line-height: 1.75;
  color: rgba(0,0,0,0.55); max-width: 500px;
  margin-bottom: var(--sp-8);
}

.newsletter__form {
  display: flex; gap: 0; max-width: 560px;
  border: 2px solid rgba(0,0,0,0.15);
}
.newsletter__input {
  flex: 1; background: rgba(0,0,0,0.04);
  border: none; outline: none;
  padding: 1rem 1.25rem;
  font-family: var(--font-body); font-size: 0.95rem; font-weight: 300;
  color: var(--black);
}
.newsletter__input::placeholder { color: rgba(0,0,0,0.35); }
.newsletter__submit {
  background: var(--red); color: white;
  padding: 0 2rem;
  font-family: var(--font-display); font-size: 0.82rem; font-weight: 800;
  letter-spacing: 0.1em; text-transform: uppercase;
  border: none; cursor: pointer;
  transition: background var(--t-fast);
}
.newsletter__submit:hover { background: var(--red-deep); }

.newsletter__note {
  margin-top: var(--sp-4);
  font-family: var(--font-mono); font-size: 0.62rem;
  letter-spacing: 0.06em; color: rgba(0,0,0,0.4);
}


/* ═══════════════════════════════════════════════════════════════
   BUTTONS
   ═══════════════════════════════════════════════════════════════ */

.btn {
  display: inline-flex; align-items: center; gap: 0.5rem;
  font-family: var(--font-display); font-weight: 700;
  letter-spacing: 0.1em; text-transform: uppercase;
  border: none; border-radius: 0; cursor: pointer;
  transition: all var(--t-base);
  padding: 0.8rem 2rem; font-size: 0.85rem;
  white-space: nowrap;
}
.btn--sm { padding: 0.55rem 1.25rem; font-size: 0.78rem; }
.btn--lg { padding: 1rem 2.5rem;    font-size: 0.92rem; }
.btn--xl { padding: 1.2rem 3rem;    font-size: 1rem; }
.btn--block { width: 100%; justify-content: center; }

.btn--red {
  background: var(--red); color: white;
}
.btn--red:hover { background: var(--red-deep); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(185,28,28,0.35); }

.btn--gold {
  background: var(--gold); color: var(--text-on-gold);
}
.btn--gold:hover { background: var(--gold-lite); transform: translateY(-2px); }

.btn--outline {
  background: transparent; color: var(--cream);
  border: 1px solid var(--border-lit);
}
.btn--outline:hover { border-color: var(--gold); color: var(--gold); background: var(--gold-mist); }

.btn--outline-dark {
  background: transparent; color: var(--black);
  border: 1.5px solid rgba(0,0,0,0.25);
}
.btn--outline-dark:hover { border-color: var(--red); color: var(--red); }

.btn__arr { transition: transform var(--t-base); }
.btn:hover .btn__arr { transform: translateX(4px); }


/* ═══════════════════════════════════════════════════════════════
   FOOTER
   ═══════════════════════════════════════════════════════════════ */

.footer {
  background: var(--charcoal);
  border-top: 1px solid var(--border);
  padding: var(--sp-16) 0 var(--sp-8);
}

.footer__top {
  display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 4rem; padding-bottom: var(--sp-12);
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--sp-6);
}

.footer__brand-abbr {
  font-family: var(--font-display); font-size: 2rem; font-weight: 900;
  letter-spacing: 0.04em; color: var(--cream); margin-bottom: 0.2rem;
}
.footer__brand-full {
  font-family: var(--font-mono); font-size: 0.58rem;
  letter-spacing: 0.14em; text-transform: uppercase;
  color: var(--text-muted); margin-bottom: var(--sp-4);
}
.footer__tagline {
  font-family: var(--font-body); font-style: italic;
  font-size: 0.92rem; font-weight: 300;
  color: var(--text-secondary); line-height: 1.7;
  max-width: 260px; margin-bottom: var(--sp-6);
}
.footer__social { display: flex; gap: 0.6rem; }
.footer__social-link {
  width: 36px; height: 36px;
  border: 1px solid var(--border);
  display: flex; align-items: center; justify-content: center;
  font-size: 0.85rem; color: var(--text-muted);
  transition: all var(--t-fast);
}
.footer__social-link:hover { border-color: var(--gold); color: var(--gold); background: var(--gold-mist); }

.footer__col-title {
  font-family: var(--font-mono); font-size: 0.62rem;
  letter-spacing: 0.2em; text-transform: uppercase;
  color: var(--gold); margin-bottom: var(--sp-5);
}
.footer__links { display: flex; flex-direction: column; gap: 0.6rem; }
.footer__link {
  font-size: 0.88rem; font-weight: 300;
  color: var(--text-muted); transition: color var(--t-fast);
}
.footer__link:hover { color: var(--cream); }

.footer__bottom {
  display: flex; align-items: center; justify-content: space-between;
  font-family: var(--font-mono); font-size: 0.62rem;
  letter-spacing: 0.1em; color: var(--text-muted);
  flex-wrap: wrap; gap: 0.5rem;
}

/* ── Accessibility ── */
:focus-visible {
  outline: 2px solid var(--gold);
  outline-offset: 3px;
}
.sr-only {
  position: absolute; width: 1px; height: 1px;
  overflow: hidden; clip: rect(0,0,0,0);
  white-space: nowrap;
}

/* ── Responsive ── */
@media (max-width: 1100px) {
  .focus__grid { display: grid; grid-template-columns: 1fr 1fr; }
  .focus-card { grid-column: auto !important; }
  .impact__grid { grid-template-columns: repeat(2,1fr); }
  .footer__top  { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
}

@media (max-width: 768px) {
  .container { padding: 0 1.25rem; }
  .nav { padding: 1rem 1.25rem; }
  .nav.scrolled { padding: 0.75rem 1.25rem; }
  .nav__links { display: none; }
  .nav__ham   { display: flex; }
  .hero__title { font-size: clamp(3.5rem, 15vw, 8rem); }
  .hero__bottom { flex-direction: column; gap: 1.5rem; }
  .hero__stripe, .hero__stripe-gold { left: 1.5rem; }
  .mission__grid { grid-template-columns: 1fr; gap: 3rem; }
  .mission__label-col { flex-direction: row; align-items: center; }
  .mission__counter { font-size: 3rem; }
  .mission__side-text { display: none; }
  .about__grid { grid-template-columns: 1fr; }
  .about__image-col { min-height: 300px; }
  .about__content { padding: 2.5rem 1.5rem; }
  .involved__grid { grid-template-columns: 1fr; }
  .focus__grid { grid-template-columns: 1fr; }
  .impact__grid { grid-template-columns: repeat(2,1fr); }
  .footer__top  { grid-template-columns: 1fr; gap: 2rem; }
  .footer__bottom { flex-direction: column; gap: 0.4rem; }
  .newsletter__form { flex-direction: column; }
  .newsletter__submit { padding: 1rem; }
  .hero__footer-bar { flex-direction: column; gap: 0.4rem; text-align: center; }
}
</style>
</head>
<body>

<!-- ══════════════════════════════════════════════════════
     NAV
══════════════════════════════════════════════════════ -->
<%
=begin%>
 <a href="#main-content" class="sr-only">Skip to main content</a>

<nav class="nav" id="nav" role="navigation" aria-label="Main navigation">
  <a href="#home" class="nav__brand">
    <span class="nav__brand-abbr">YBNC</span>
    <span class="nav__brand-full">Young Black Nameless Coalition</span>
  </a>

  <div class="nav__links">
    <a href="#mission"  class="nav__link">Mission</a>
    <a href="#focus"    class="nav__link">Focus Areas</a>
    <a href="#about"    class="nav__link">Ashley</a>
    <a href="#events"   class="nav__link">Events</a>
    <a href="#involved" class="nav__link">Get Involved</a>
    <a href="#involved" class="nav__cta">Join the Coalition</a>
  </div>

  <button class="nav__ham" onclick="toggleNav()" aria-label="Open navigation" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
</nav> 
<%
=end%>


<!-- ══════════════════════════════════════════════════════
     HERO
══════════════════════════════════════════════════════ -->
<main id="main-content">
<section class="hero" id="home">
  <div class="hero__stripe"  aria-hidden="true"></div>
  <div class="hero__stripe-gold" aria-hidden="true"></div>
  <div class="hero__glow"    aria-hidden="true"></div>

  <div class="hero__content">
    <div class="hero__mono-label mono-label">
      Vallejo, California · Est. 2020 · Black Liberation
    </div>

    <h1 class="hero__title" aria-label="The movement has a name.">
      <span class="hero__title-line">
        <span class="hero__title-word">The</span>
      </span>
      <span class="hero__title-line">
        <span class="hero__title-word">Movement</span>
      </span>
      <span class="hero__title-line">
        <span class="hero__title-word">Has a</span>
      </span>
      <span class="hero__title-line">
        <span class="hero__title-word"><em>Name.</em></span>
      </span>
    </h1>

    <div class="hero__bottom">
      <p class="hero__desc">
        <strong>Young Black Nameless Coalition</strong> — empowering young Black individuals, dismantling systemic barriers, and amplifying Black voices. Community-driven. Unapologetic. Built for liberation.
      </p>
      <div class="hero__actions">
        <a href="#involved" class="btn btn--red btn--xl">
          Join the Coalition <span class="btn__arr">→</span>
        </a>
        <a href="#mission" class="btn btn--outline btn--xl">Our Mission</a>
      </div>
    </div>
  </div>

  <div class="hero__footer-bar" role="complementary">
    <span class="hero__footer-bar-text">
      Founded by <strong>Ashley Dorelus</strong> · Documentary Filmmaker · Social Justice Advocate · Community Hero Award Recipient
    </span>
    <span class="hero__footer-bar-text">
      🇭🇹 Vallejo, CA · Active since <strong>May 29, 2020</strong>
    </span>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     MARQUEE
══════════════════════════════════════════════════════ -->
<div class="marquee" aria-hidden="true">
  <div class="marquee__track">
    <span class="marquee__item marquee__item--gold">Black Liberation</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Community Organizing</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Political Advocacy</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Systemic Change</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Amplify Black Voices</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Youth Empowerment</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Social Justice</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Unity &amp; Action</span>
    <span class="marquee__sep">✦</span>
    <!-- Duplicate for seamless loop -->
    <span class="marquee__item marquee__item--gold">Black Liberation</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Community Organizing</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Political Advocacy</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Systemic Change</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Amplify Black Voices</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Youth Empowerment</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item marquee__item--gold">Social Justice</span>
    <span class="marquee__sep">✦</span>
    <span class="marquee__item">Unity &amp; Action</span>
    <span class="marquee__sep">✦</span>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════
     MISSION
══════════════════════════════════════════════════════ -->
<section class="mission section" id="mission">
  <div class="container">
    <div class="mission__grid">

      <div class="mission__label-col reveal">
        <div class="mission__counter" aria-hidden="true">01</div>
        <div class="mono-label" style="writing-mode: horizontal-tb; margin-bottom: var(--sp-4);">Our Mission</div>
        <div class="mission__side-text" aria-hidden="true">
          YBNC · VALLEJO, CA · BLACK LIBERATION
        </div>
      </div>

      <div>
        <div class="mono-label reveal">Mission Statement</div>
        <h2 class="mission__statement reveal d1">
          Fostering unity and empowerment —
          <em>helping Black individuals thrive</em>
          and contribute to a more equitable society.
        </h2>

        <p class="mission__body reveal d2">
          The Young Black Nameless Coalition was born from a simple, radical idea: that the people historically erased from power deserve a platform built in their name. We organize, advocate, document, and amplify — because representation without action is just performance.
        </p>

        <p class="mission__body reveal d2">
          We center the experiences of young Black individuals in every decision we make. From street-level advocacy to policy change, from community care to political organizing — YBNC exists at the intersection of where movement-building actually happens.
        </p>

        <blockquote class="mission__pull-quote reveal d3">
          <p class="mission__pull-quote-text">
            "We will be here every day and every night until we see some justice. The support in the comments is not the same as tangible support in the form of showing up."
          </p>
          <footer class="mission__pull-quote-attr">
            — Ashley Dorelus, Founder · YBNC
          </footer>
        </blockquote>
      </div>

    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     FOCUS AREAS
══════════════════════════════════════════════════════ -->
<section class="focus section" id="focus">
  <div class="container" style="padding: 0;">
    <div style="padding: var(--sp-8);">
      <div class="mono-label reveal">Focus Areas</div>
      <h2 class="headline reveal d1" style="font-size: clamp(2.5rem, 6vw, 5rem); color: var(--cream); margin-top: var(--sp-3);">
        What We<br><span style="color: var(--red);">Fight For.</span>
      </h2>
    </div>

    <div class="focus__grid" role="list">

      <div class="focus-card focus-card--1 focus-card--accent reveal" data-bg="VOTE" role="listitem">
        <div class="focus-card__num">01 · Political Advocacy</div>
        <div class="focus-card__icon" aria-hidden="true">🗳️</div>
        <h3 class="focus-card__title">Political Advocacy &amp; Voting Rights</h3>
        <p class="focus-card__desc">Engaging in civic action, voter registration drives, policy advocacy, and holding elected officials accountable to Black communities.</p>
      </div>

      <div class="focus-card focus-card--2 reveal d1" data-bg="BUILD" role="listitem">
        <div class="focus-card__num">02 · Community Action</div>
        <div class="focus-card__icon" aria-hidden="true">✊</div>
        <h3 class="focus-card__title">Community Organizing &amp; Action</h3>
        <p class="focus-card__desc">Grassroots organizing, mutual aid, community events, and building solidarity networks that create lasting change from the ground up.</p>
      </div>

      <div class="focus-card focus-card--3 focus-card--gold-accent reveal d2" data-bg="FREE" role="listitem">
        <div class="focus-card__num">03 · Liberation</div>
        <div class="focus-card__icon" aria-hidden="true">✨</div>
        <h3 class="focus-card__title">Black Liberation &amp; Freedom</h3>
        <p class="focus-card__desc">Centering Black liberation as the foundational goal. Everything we do serves the larger arc of dismantling oppression.</p>
      </div>

      <div class="focus-card focus-card--4 reveal d1" data-bg="RISE" role="listitem">
        <div class="focus-card__num">04 · Empowerment</div>
        <div class="focus-card__icon" aria-hidden="true">🌱</div>
        <h3 class="focus-card__title">Youth Empowerment &amp; Development</h3>
        <p class="focus-card__desc">Investing in young Black leaders through mentorship, resources, leadership training, and creating pathways to opportunity.</p>
      </div>

      <div class="focus-card focus-card--5 reveal d2" data-bg="TRUTH" role="listitem">
        <div class="focus-card__num">05 · Documentation</div>
        <div class="focus-card__icon" aria-hidden="true">🎥</div>
        <h3 class="focus-card__title">Documentary &amp; Media Justice</h3>
        <p class="focus-card__desc">On-the-ground documentation of social injustice. Telling the correct narrative across 20+ states and dozens of cities — frontline media as resistance.</p>
      </div>

      <div class="focus-card focus-card--6 reveal d3" data-bg="BREAK" role="listitem">
        <div class="focus-card__num">06 · Systemic Change</div>
        <div class="focus-card__icon" aria-hidden="true">⚖️</div>
        <h3 class="focus-card__title">Dismantling Systemic Barriers</h3>
        <p class="focus-card__desc">Challenging structural racism in policy, criminal justice, education, and economics — because individual uplift without systemic change is incomplete.</p>
      </div>

    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     ABOUT ASHLEY
══════════════════════════════════════════════════════ -->
<section class="about section" id="about">
  <div class="container" style="padding: 0;">
    <div class="about__grid">

      <div class="about__image-col reveal">
        <div class="about__photo-bg"></div>
        <div class="about__photo-frame">
          <div class="about__photo-corner about__photo-corner--tl"></div>
          <div class="about__photo-corner about__photo-corner--tr"></div>
          <div class="about__photo-corner about__photo-corner--bl"></div>
          <div class="about__photo-corner about__photo-corner--br"></div>
        </div>
        <div class="about__photo-placeholder" aria-hidden="true">✊🏾</div>

        <!-- Replace with: <%#= image_tag ashley_photo_path, alt: "Ashley Dorelus, Founder of YBNC" %> -->

        <div class="about__award">
          <div class="about__award-icon" aria-hidden="true">🏆</div>
          <div>
            <div class="about__award-title">Recipient</div>
            <div class="about__award-name">Community Hero Award · Ashley Dorelus</div>
          </div>
        </div>
      </div>

      <div class="about__content">
        <div class="mono-label reveal">Meet the Founder</div>
        <h2 class="about__title reveal d1">
          Ashley<br><em>"Aesthetics"</em><br>Dorelus
        </h2>

        <p class="about__body reveal d2">
          Ashley Dorelus is a documentary filmmaker, social justice advocate, and community organizer based in Vallejo, California. She has been on the frontlines since May 29, 2020 — traveling to over 20 states and dozens of cities to document the uprising at the intersection of two pandemics: COVID-19 and the ongoing genocide of Black people at the hands of police.
        </p>

        <p class="about__body reveal d2">
          From Oakland to Minneapolis, Philadelphia to Atlanta — Ashley's lens captured what mainstream media refused to show. Her work, shared widely after Amanda Seals amplified her documentation of the Daunte Wright shooting, helped galvanize national attention to frontline journalism by community members.
        </p>

        <p class="about__body reveal d2">
          A 🇭🇹 Haitian-American, Ashley received a Community Hero Award for her work and founded YBNC as a vehicle for turning documentation into organized, sustained action. She believes community-driven change begins with showing up.
        </p>

        <div class="about__tags reveal d3">
          <span class="about__tag">Documentary Filmmaker</span>
          <span class="about__tag">Community Organizer</span>
          <span class="about__tag">Social Justice Advocate</span>
          <span class="about__tag">Frontline Journalist</span>
          <span class="about__tag">Haitian-American</span>
          <span class="about__tag">Community Hero Awardee</span>
          <span class="about__tag">Pro-Black Liberation</span>
        </div>

        <div class="about__links reveal d4">
          <a href="https://www.instagram.com/aestheticsconash/" target="_blank" rel="noopener noreferrer" class="btn btn--outline">
            📸 @aestheticsconash
          </a>
          <a href="#involved" class="btn btn--red">
            Support the Work →
          </a>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     IMPACT STATS
══════════════════════════════════════════════════════ -->
<section class="impact" aria-label="Our impact">
  <div class="container" style="padding: 0;">
    <div class="impact__grid">
      <div class="impact-stat reveal">
        <div class="impact-stat__num" data-count="20">20+</div>
        <div class="impact-stat__label">States Documented</div>
        <div class="impact-stat__desc">Frontline coverage of the uprising across America</div>
      </div>
      <div class="impact-stat reveal d1">
        <div class="impact-stat__num">2020</div>
        <div class="impact-stat__label">Year Founded</div>
        <div class="impact-stat__desc">Active since the uprising began on May 29</div>
      </div>
      <div class="impact-stat reveal d2">
        <div class="impact-stat__num" data-count="19">19K+</div>
        <div class="impact-stat__label">Community Followers</div>
        <div class="impact-stat__desc">And growing — building power through platform</div>
      </div>
      <div class="impact-stat reveal d3">
        <div class="impact-stat__num">∞</div>
        <div class="impact-stat__label">Commitment to Justice</div>
        <div class="impact-stat__desc">Until we see systemic change, we don't stop</div>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     EVENTS
══════════════════════════════════════════════════════ -->
<section class="events section" id="events">
  <div class="container">
    <div class="events__header">
      <div>
        <div class="mono-label reveal">On the Ground</div>
        <h2 class="headline reveal d1" style="font-size: clamp(2.5rem, 5vw, 4.5rem); color: var(--cream); margin-top: var(--sp-3);">
          Upcoming<br><span style="color: var(--gold);">Actions &amp; Events.</span>
        </h2>
      </div>
      <a href="#" class="btn btn--outline reveal d2">View All Events →</a>
    </div>

    <div class="events__list" role="list">

      <div class="event-row reveal" role="listitem">
        <div class="event-row__date" aria-label="April 19">
          <div class="event-row__date-day">19</div>
          <div class="event-row__date-month">Apr</div>
        </div>
        <div class="event-row__info">
          <div class="event-row__title">Community Town Hall — Vallejo</div>
          <div class="event-row__meta">
            <span>6:00 PM · Vallejo City Hall</span>
            <span>Open to all</span>
          </div>
        </div>
        <span class="event-type--advocacy event-row__type">Advocacy</span>
      </div>

      <div class="event-row reveal d1" role="listitem">
        <div class="event-row__date" aria-label="April 26">
          <div class="event-row__date-day">26</div>
          <div class="event-row__date-month">Apr</div>
        </div>
        <div class="event-row__info">
          <div class="event-row__title">Youth Empowerment Workshop</div>
          <div class="event-row__meta">
            <span>2:00 PM · TBD · Vallejo</span>
            <span>Ages 16–25</span>
          </div>
        </div>
        <span class="event-type--community event-row__type">Community</span>
      </div>

      <div class="event-row reveal d2" role="listitem">
        <div class="event-row__date" aria-label="May 3">
          <div class="event-row__date-day">03</div>
          <div class="event-row__date-month">May</div>
        </div>
        <div class="event-row__info">
          <div class="event-row__title">Know Your Rights — Community Training</div>
          <div class="event-row__meta">
            <span>4:00 PM · Online &amp; In-Person</span>
            <span>Free · RSVP required</span>
          </div>
        </div>
        <span class="event-type--education event-row__type">Education</span>
      </div>

      <div class="event-row reveal d2" role="listitem">
        <div class="event-row__date" aria-label="May 17">
          <div class="event-row__date-day">17</div>
          <div class="event-row__date-month">May</div>
        </div>
        <div class="event-row__info">
          <div class="event-row__title">Documentary Screening — The Uprising</div>
          <div class="event-row__meta">
            <span>7:00 PM · Vallejo</span>
            <span>Free · Community Discussion to follow</span>
          </div>
        </div>
        <span class="event-type--community event-row__type">Community</span>
      </div>

      <div class="event-row reveal d3" role="listitem">
        <div class="event-row__date" aria-label="May 29">
          <div class="event-row__date-day">29</div>
          <div class="event-row__date-month">May</div>
        </div>
        <div class="event-row__info">
          <div class="event-row__title">5th Anniversary — Day of Action &amp; Commemoration</div>
          <div class="event-row__meta">
            <span>All Day · Multiple Locations</span>
            <span>5 Years since the uprising began · <strong style="color:var(--red);">Mark your calendar</strong></span>
          </div>
        </div>
        <span class="event-type--advocacy event-row__type">Advocacy</span>
      </div>

    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     GET INVOLVED
══════════════════════════════════════════════════════ -->
<section class="involved section" id="involved">
  <div class="container">
    <div class="involved__header reveal">
      <div class="mono-label" style="justify-content: center; margin-bottom: var(--sp-3);">Take Action</div>
      <h2 class="headline" style="font-size: clamp(2.5rem, 6vw, 5.5rem); color: var(--cream);">
        How You <span style="color: var(--red);">Show Up.</span>
      </h2>
      <p style="margin-top: var(--sp-4); font-style: italic; color: var(--text-secondary); font-size: 1rem;">
        The comments section is not the same as showing up. Here's how to make it tangible.
      </p>
    </div>

    <div class="involved__grid">

      <div class="involve-card reveal">
        <div class="involve-card__num">01 / Join</div>
        <div class="involve-card__icon" aria-hidden="true">✊</div>
        <h3 class="involve-card__title">Join the Coalition</h3>
        <p class="involve-card__desc">
          Become a member of YBNC. Show up to meetings, events, and actions. Be part of the organizing infrastructure that makes sustained change possible.
        </p>
        <a href="#newsletter" class="btn btn--red">Become a Member →</a>
      </div>

      <div class="involve-card involve-card--donate reveal d1">
        <div class="involve-card__num">02 / Donate</div>
        <div class="involve-card__icon" aria-hidden="true">💛</div>
        <h3 class="involve-card__title">Fund the Work</h3>
        <p class="involve-card__desc">
          Tangible support — financial contributions, sponsorships, and partnerships — fuel everything. Frontline documentation, events, resources, advocacy all require investment.
        </p>
        <a href="#" class="btn btn--gold">Donate Now →</a>
      </div>

      <div class="involve-card reveal d2">
        <div class="involve-card__num">03 / Volunteer</div>
        <div class="involve-card__icon" aria-hidden="true">🌱</div>
        <h3 class="involve-card__title">Volunteer Your Skills</h3>
        <p class="involve-card__desc">
          Legal support, graphic design, event organizing, social media, outreach, translation, journalism — bring what you have and put it in service of the community.
        </p>
        <a href="#newsletter" class="btn btn--outline">Get Involved →</a>
      </div>

    </div>

    <!-- Contact / Partnerships box -->
    <div style="margin-top: 2rem; padding: 2.5rem; background: var(--dark); border: 1px solid var(--border); display: flex; align-items: center; justify-content: space-between; gap: 3rem; flex-wrap: wrap;" class="reveal">
      <div>
        <div class="mono-label" style="margin-bottom: var(--sp-3);">Partnerships &amp; Collaborations</div>
        <p style="font-size: 0.95rem; font-weight: 300; color: var(--text-secondary); max-width: 500px; line-height: 1.7;">
          Organizations, media outlets, lawyers, funders, and community groups — reach out to discuss how we can build together. Coalition work requires coalition.
        </p>
      </div>
      <a href="#newsletter" class="btn btn--outline btn--lg">Get In Touch →</a>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════
     NEWSLETTER / JOIN
══════════════════════════════════════════════════════ -->
<section class="newsletter" id="newsletter" aria-label="Join the YBNC community">
  <div class="container">
    <div class="newsletter__inner">
      <div class="newsletter__label">Stay Connected</div>
      <h2 class="newsletter__title">
        Be Part<br>of the <em>Fight.</em>
      </h2>
      <p class="newsletter__sub">
        Get updates on actions, events, and how to plug in. No spam — just the movement news that matters.
      </p>
      <form class="newsletter__form" onsubmit="handleNewsletter(event)" aria-label="Newsletter signup">
        <input type="email" class="newsletter__input" placeholder="Your email address" required aria-label="Email address">
        <button type="submit" class="newsletter__submit">Join YBNC →</button>
      </form>
      <p class="newsletter__note">Your information will never be sold or shared. This is a community, not a product.</p>
    </div>
  </div>
</section>
</main>


<!-- ══════════════════════════════════════════════════════
     FOOTER
══════════════════════════════════════════════════════ -->
<footer class="footer" role="contentinfo">
  <div class="container">
    <div class="footer__top">
      <div>
        <div class="footer__brand-abbr">YBNC</div>
        <div class="footer__brand-full">Young Black Nameless Coalition</div>
        <p class="footer__tagline">"Named by the unnamed. Built for the unseen. Fighting for the liberation of all Black people."</p>
        <div class="footer__social" aria-label="Social media links">
          <a href="https://www.instagram.com/aestheticsconash/" target="_blank" rel="noopener noreferrer" class="footer__social-link" title="Instagram" aria-label="Instagram">📸</a>
          <a href="https://www.linkedin.com/in/young-black-nameless-coalition-7571ab303" target="_blank" rel="noopener noreferrer" class="footer__social-link" title="LinkedIn" aria-label="LinkedIn">💼</a>
          <a href="https://www.instagram.com/reels/CzT9_pXpOER/" target="_blank" rel="noopener noreferrer" class="footer__social-link" title="Watch our reel" aria-label="Instagram Reel">🎥</a>
        </div>
      </div>

      <div>
        <div class="footer__col-title">Organization</div>
        <ul class="footer__links">
          <li><a href="#mission"  class="footer__link">Our Mission</a></li>
          <li><a href="#focus"    class="footer__link">Focus Areas</a></li>
          <li><a href="#about"    class="footer__link">About Ashley</a></li>
          <li><a href="#events"   class="footer__link">Events</a></li>
          <li><a href="#involved" class="footer__link">Get Involved</a></li>
        </ul>
      </div>

      <div>
        <div class="footer__col-title">Take Action</div>
        <ul class="footer__links">
          <li><a href="#involved"  class="footer__link">Join the Coalition</a></li>
          <li><a href="#involved"  class="footer__link">Donate</a></li>
          <li><a href="#involved"  class="footer__link">Volunteer</a></li>
          <li><a href="#newsletter"class="footer__link">Newsletter</a></li>
          <li><a href="#involved"  class="footer__link">Partnerships</a></li>
        </ul>
      </div>

      <div>
        <div class="footer__col-title">Contact</div>
        <ul class="footer__links">
          <li><a href="https://www.instagram.com/aestheticsconash/" target="_blank" rel="noopener noreferrer" class="footer__link">@aestheticsconash</a></li>
          <li><span class="footer__link" style="cursor:default;">Vallejo, CA 94589</span></li>
        </ul>
        <div style="margin-top: var(--sp-6);">
          <a href="#involved" class="btn btn--red btn--sm">Join Now →</a>
        </div>
      </div>
    </div>

    <div class="footer__bottom">
      <span>© 2025 Young Black Nameless Coalition, Inc. · Vallejo, California</span>
      <span>Founded by Ashley Dorelus · All power to the people · ✊🏾</span>
    </div>
  </div>
</footer>


<!-- ══════════════════════════════════════════════════════
     JAVASCRIPT
══════════════════════════════════════════════════════ -->
<script>
/* ── Nav scroll ── */
const nav = document.getElementById('nav');
window.addEventListener('scroll', () => {
  nav.classList.toggle('scrolled', window.scrollY > 60);
}, { passive: true });

/* ── Scroll reveal ── */
const obs = new IntersectionObserver(entries => {
  entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('vis'); });
}, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
document.querySelectorAll('.reveal').forEach(el => obs.observe(el));

/* ── Mobile nav ── */
function toggleNav() {
  const links  = document.querySelector('.nav__links');
  const btn    = document.querySelector('.nav__ham');
  const isOpen = links.dataset.open === '1';

  if (isOpen) {
    links.style.cssText = '';
    links.dataset.open  = '0';
    btn.setAttribute('aria-expanded', 'false');
  } else {
    links.style.cssText = `
      display:flex; flex-direction:column; align-items:center; justify-content:center;
      gap:2.5rem; position:fixed; inset:0;
      background:rgba(10,10,10,0.98); backdrop-filter:blur(16px);
      z-index:300; animation:navIn 0.3s ease;
    `;
    links.dataset.open = '1';
    btn.setAttribute('aria-expanded', 'true');
    links.querySelectorAll('.nav__link, .nav__cta').forEach(a => {
      a.style.cssText = 'font-size:2.2rem;font-weight:800;text-transform:uppercase;letter-spacing:.06em;color:var(--cream);font-family:"Syne",sans-serif;';
      a.addEventListener('click', () => {
        links.style.cssText = '';
        links.dataset.open  = '0';
        btn.setAttribute('aria-expanded', 'false');
      }, { once: true });
    });
  }
}

/* ── Newsletter ── */
function handleNewsletter(e) {
  e.preventDefault();
  const btn = e.target.querySelector('.newsletter__submit');
  const original = btn.textContent;
  btn.textContent = '✓ You\'re in. See you on the frontlines.';
  btn.style.background = '#991818';
  e.target.querySelector('.newsletter__input').value = '';
  setTimeout(() => { btn.textContent = original; btn.style.background = ''; }, 5000);
}

const s = document.createElement('style');
s.textContent = '@keyframes navIn { from{opacity:0} to{opacity:1} }';
document.head.appendChild(s);
</script>
</body>
</html>
