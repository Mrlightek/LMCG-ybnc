<%= form_with(model: volunteer_application) do |form| %>
  <% if volunteer_application.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(volunteer_application.errors.count, "error") %> prohibited this volunteer_application from being saved:</h2>

      <ul>
        <% volunteer_application.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
